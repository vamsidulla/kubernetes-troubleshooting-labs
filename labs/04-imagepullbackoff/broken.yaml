apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-pull-lab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: image-pull-lab
  template:
    metadata:
      labels:
        app: image-pull-lab
    spec:
      containers:
      - name: app
        image: busybox:deliberately-invalid-k8s-lab-tag
        command:
        - sh
        - -c
        - while true; do sleep 3600; done
